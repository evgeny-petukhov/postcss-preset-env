{
  "name": "postcss-preset-env-codepen",
  "version": "1.0.0",
  "scripts": {
    "browser": "browserify browser.js | babel --presets=env | uglifyjs --compress --mangle > browser.bundle.js; gzip-size browser.bundle.js",
    "start": "browserify codepen.js | babel --presets=env | uglifyjs --compress --mangle > codepen.bundle.js; gzip-size codepen.bundle.js"
  },
  "dependencies": {
    "browserslist": "^2.11",
    "postcss": "^6.0.16",
    "postcss-preset-env": "file:.."
  },
  "devDependencies": {
    "babel-cli": "^6.26.0",
    "browserify": "^15.2.0",
    "gzip-size-cli": "^2.1.0",
    "uglify-js": "^3.3.9"
  },
  "private": true
}
